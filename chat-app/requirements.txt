# Flask - The web framework that powers our application
# Version 3.0.0 is the latest stable release with modern Python support
Flask==3.0.0

# Flask-CORS - Handles Cross-Origin Resource Sharing
# This is needed so our frontend JavaScript can talk to our backend API
# Even though they're in the same container, this is good practice
flask-cors==4.0.0

# Werkzeug - Flask's underlying WSGI toolkit
# We specify this to ensure compatibility with our Flask version
Werkzeug==3.0.1