# Docker Setup Documentation

NOTE: make sure you have bout .env files in the root directory and in the agent directory

if thats the case run the following commands:

```bash
docker compose up --build
```

and the web page should be in http://localhost:5000